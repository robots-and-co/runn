{
  "name": "runn",
  "version": "0.0.1",
  "private": true,
  "description": "Personal task runner — boards as workspaces, parent cards as goals, Claude as the engine.",
  "scripts": {
    "start": "node worker/server.js",
    "dev": "node --watch worker/server.js"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "ws": "^8.18.0"
  }
}
